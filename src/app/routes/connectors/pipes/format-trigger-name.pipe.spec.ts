import { FormatTriggerNamePipe } from './format-trigger-name.pipe';

describe('FormatTriggerNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTriggerNamePipe();
    expect(pipe).toBeTruthy();
  });
});
